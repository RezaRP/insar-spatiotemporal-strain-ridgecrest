# Runtime + analysis dependencies (pip route).
# Preferred install is `conda env create -f environment.yml`; this file is a fallback
# for CI and for users without conda. Pinned to major versions only, so that a future
# reader can still resolve an environment without being locked to 2026 patch releases.
h5py>=3.10
numpy>=1.26
pandas>=2.1
scipy>=1.11
matplotlib>=3.8
pillow>=10.0
pyproj>=3.6
scikit-learn>=1.3
tifffile>=2024.1.30
